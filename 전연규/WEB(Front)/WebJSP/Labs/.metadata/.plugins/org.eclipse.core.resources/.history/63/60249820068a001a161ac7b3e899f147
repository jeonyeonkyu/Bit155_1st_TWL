package kr.or.bit.utils;

import java.util.ArrayList;
import java.util.List;

public class DTO {
	
	class KoreaMember{
		
	}

	Listt<KoreaMember> introList = new ArrayList()<KoreaMember>();

	while(rs.next()){
	Member m = new Member();
	m.setEmp(rs.getInt("empno"))
	introList.add(m)
	}

	<c:set var="list" value="<%= introlist %>">
	<c:forEach var ="member" items="${list}">
	${member.empno}


	
}
