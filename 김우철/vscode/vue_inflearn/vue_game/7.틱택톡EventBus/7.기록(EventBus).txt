이벤트버스(이벤트를 중앙에서 통제 / 많이 쓰임)

EventBus.js 만들기

        created() {
            //사용자 정의 이벤트 등록
            EventBus.$on('clickTd', this.onClickTd);
        }
>>TicTacToc.vue

        methods:{
            onClickTd(){
                EventBus.$emit('clickTd');
            }
            }
>>TdComponent.vue

가장 최상위 컴포넌트에서 모든것을 다 처리

rootData필요없고 this.tableData로 바꾸기

단점 : root컴포넌트가 너무 길어짐




