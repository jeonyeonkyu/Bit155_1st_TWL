시간이 지난후 자신의 코드가 남의코드처럼 느껴진다면 
코드잘못짠거임

후보칸 = 후보칸.filter(function(칸) {
  return !칸.textContent;
}); 
=> filter 메소드는 return값이 true인 애들만 걸러냄
=> 66줄의 칸.textContent = "";에 의해 !를 붙여줘야됨(?)


대각선 검사 부분 리펙토링
=> 그냥 모든 칸을 누를 때마다 대각선 검사를 하도록 함

배열에서만 indexof를 사용할 수 있다.

컴퓨터의 턴일동안은 클릭못하게 방지

