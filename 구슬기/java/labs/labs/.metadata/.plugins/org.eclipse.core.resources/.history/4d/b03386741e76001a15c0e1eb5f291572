var 비동기콜백 = function(이벤트) {
  console.log(이벤트.target);
};

이벤트.targe을 하면
클릭한 부분을 인식

이벤트.target == 클릭된 애
이벤트.target.parentNode == 클릭된 애 부모 태그
여기서는 td의 부모인 tr

이벤트.target.children == 클릭된애 자식 태그

배열의 반복문 foreach

배열안에 있는 값을 요소(엘리먼트)라 칭함

배열이나 객체는 데이터를 표현할 때 많이 씀

중첩 반복문 횟수를 줄이는게 실력(최대한 
-> 알고리즘에서 반복문 3번이상 쓰면 안좋음
-> 어쩔수 없이 3개를 써야한다는 것은 ㄱㅊ
-> 최대한 줄이는게 포인트
