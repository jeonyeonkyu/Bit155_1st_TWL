<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>끝말잇기</title>
    <script src="https://cdn.jsdelivr.net/npm/vue/dist/vue.js"></script>
</head>

<body>
    <div id="root">
        <word-relay start-word="제로초"></word-relay>
        <word-relay start-word="초밥"></word-relay>
        <word-relay start-word="바보"></word-relay>
        <!-- 이 3개의 컴포넌트의 데이터는 따로 놈 -->
        <!-- 컴포넌트를 쓰는쪽에서 props 데이터를 넣어줌 -->
    </div>
</body>

<!-- 컴포넌트가 가장 상단에 들어감 -->
<script> //핵심적인것은 컴포넌트게 들어감
// 전역 컴포넌트
    Vue.component('word-relay', {
        //백틱 사용(ES6)
        template: ` 
        <div>
            <div id="root">
                <div>{{word}}</div>
                    <form v-on:submit="onSubmitForm">
                        <input type="text" ref="answer" v-model="value">
                        <button type="submit">입력</button>
                    </form>
                <div id="result">{{result}}</div>
            </div>
        </div>
        `,
        props: ['startWord'],
        data() {
            return {
                word: this.startWord,
                result: '',
                value: '',
            };
        },
        methods: {
            onSubmitForm(e) {
                e.preventDefault(); //form의 기본적인 새로고침 막음
                console.log()
                if (this.word[this.word.length - 1] === this.value[0]) {
                    this.word = this.value;
                    this.value = "";
                    this.$refs.answer.focus();
                    this.result = "딩동댕";
                    //input태그 선택
                } else {
                    this.value = '';
                    this.result = "땡z";
                }
            }
        }
    })
</script>

<script> //Vue 인스턴스는 간편해짐
    const app = new Vue({
        el: '#root',
    })
</script>

</html>