<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Insert title here</title>
	<script type ="text/javascript">
		//javascript 해석 : 웹 브라우저 (해석기 + 다양한 객체 제공)
		//웹 브라우져 내장 객체 (window)
	    //window 객체를 생략해서 쓴다..
		window.document.write("방가방가 <br> 라고 웹브라우저에 쓰는거임 태그도 먹는다.<br>")
	    document.write("window 객체 생략가능<br>")
	    
	    console.log(10+10);
	    console.log("디버깅, 결과확인, 오류메시지등을 확인");
	    
	    document.write("<b>hello world</b>");  //<b> 는 적용되어서 글씨가 두껍게 나온다. 
	    document.write('<table border="1"');
	    	document.write('<tr><td>AA</td><td>BB</td></tr>');
	    document.write('</table>');   //브라우저가 화면에 쓰게 되면 해석할수있는건 해석하고, 그 결과를 출력해주는 구나!!!
	    
	    var num; //javascript 는 미리 타입을 정의 하지 않아요. 
	    // int num;
	    // float num;  
	    // String num;   처럼 자바는 명확한데 반해.
	    console.log(num);   // undefined    이건 에러가 아니라. 선언은 했지만 정의되지않았음. 이라는 뜻이다. 
	    
	    num =100; // 이때 얘들은 타입이 설정된다.   (값을 가지는 순간)  유연한방법이다. 
	    console.log(num);  // 100 
	    console.log(typeof(num));   //number     ...  typeof() 는 무슨 타입인지 알려주는 함수. 
	    
	    //순서에 대한 수업 ******************************************
	    //var ele = window.document.forms[0].elements;   //form 0번째 안에 있는 요소들 (elements) 중에서 0번째 정보를 읽겟다. 그걸 변수에 담겠다.
	    //console.log(ele);
	    //Uncaught TypeError: Cannot read property 'elements' of undefined  
	    //에러가 뜨는 이유는 뭘까?  자바 스크립트는 위에서 부터 읽으니까 아직 정의된 내용이 없다. (폼은 저 아래에 있다)  아직 없는거 가지고 읽으려해서 문제됨. 
	    //객체가 올라가기 전에 읽으려해서 오류나는 거임.
	    //바디아래로 옮기니까 된다.
	    //순서에 대한 수업 ******************************************
	    
	    //순서에 상관없이 처리하고 싶을땐 (함수를 만들고 호출)
	    //이렇게 만들어놓으면 함수를 호출할대 처리됨. 순서상관없음 
	    function data(){
	    	var ele = window.document.forms[0].elements;   //form 0번째 안에 있는 요소들 (elements) 중에서 0번째 정보를 읽겟다. 그걸 변수에 담겠다.
	    	console.log(ele);
	    }
	    //순서에 대한 수업 ******************************************
	    
	</script>
</head>
<body>
		<form action="#" method="get" name="myform">
			<input type = "text"      name="userid" value="hong"><br>
			<input type = "password"  name="pwd"    value="1004"><br>
			<input type = "submit"    value="전송">
		</form>
		<input type="button" value="클릭해봐" onclick="data()">
</body>

<script type="text/javascript">
var ele = window.document.forms[0].elements[0];
console.log(ele); //ele 가지고 있는 것은 : <input type="text" name="userid" value="hong">
console.log(ele.value);
console.log(ele.value.length);       //갯수를 세는함수도 쓸수있다. 
	
	//form 태그가 자신의 이름 을 가지고 있다 >> name="myform"
	//window.document.폼이름.요소이름
	var ele2 = window.document.myform.userid;
	console.log("ele2: "+ele2);
	console.log("ele2 value : "+ele2.value);
	
	var value = window.document.myform.userid.value;
	cnsole.log("타입 : "+ typeof(value));
	
	if(value.length < 5){
		alert("다시 입력해");
		ele2.value="";
	}  //쭉 해석해서 밸류값을 넣고 타입본다음에 길이가 5보다 작다면 다시 입력해라고 함. 
</script>

</html>






















