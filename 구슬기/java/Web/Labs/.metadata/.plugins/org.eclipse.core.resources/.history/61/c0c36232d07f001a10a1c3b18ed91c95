<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Insert title here</title>
	<script type="text/javascript">
	
		var myTime= window.setInterval(function){                           //setTime , setInterval 자주 쓰임. 
			//javascript 다양한 객체 제공 받아요..웹브라우저한테서..
			var d = new Date();  //javascript 도 new 가 잇음. 필요하면 생성해서 쓰세요. 
			var t = d.toLocaleTimeString();  //현재 pc에 맞는 시간이 제공됨.
			document.getElementById("time").innerHTML =t;   
		},1000);  //익명함수활용 

		//var myTime= window.setInterval(function(){},1000);
		//function(){} 익명함수
		//1초마다 익명함수를 호출해라  . 1초 간격마다 함수를 호출해라. 
		function myTimeStop(){
			window.clearInterval(myTime);        // myTime변수 안에 있는 정보를 지워버려라.
		}
	</script>
</head>
<body>
		<p id="time"></p>
		<button onclick="myTimeStop()">Stop Time</button>   <!-- 1초에 한번씩 호출되던 시간 객체를 myTimeStop() 함수로 멈췄다. -->
</body>
</html>