<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Insert title here</title>
    <script type"text/javascript">
        /*
        -변수생성> 타입설정 > 초기화
        -모든 변수의 타입(var) ES5 : 이전 ES 2015 (ES 6) 이후 (let, const 를 만들었다.)
        -block scope 를 가진 let과 const 키워드  (얘네는 나중에 더 정리~~~~~~~해주심~~~~`)
        -var 함수(scope) 함수안에서는 얘가 짱! 
        -ECMA 2017 (ES 8) 비동기 ......화면 깜빡임없이 데이터 처리되는것.   동기와 비동기 차이 알아보자~ 
        
        var a;
        let b;
        const c;


        -String , Number 타입이 있을까요?   네, 있습니다
        -var ename;  지금은 undifined   정의되지 않았다. 오류는 아님
        -ename ="홍길동"  >> 값설정 한 지금 타입설정 된다  >> String 
        -ename =100       >> 값설정 한 지금 타입설정 된다  >> Number
        -초기화 시점에 타입이 설정된다 (처음 값을 갖는 것을 초기화)
        
        -javascript (정수 , 실수, boolean, 문자)  
        -내부적으로 >> Number ,Boolean, Array .Object...   얘네도 타입으로 본다

        변수의 범위 :     page >  function


var num;   //instance var  정도        page 
        function call(){
            let name;   //local 정도....       function    함수안에서는 let으로 만든다. 
            name ="방가";   //얘는 String 이 되고.

            num =100;       //얘는 Number 되고. 
        }



        */  
       var a;
       console.log(a); //undefined
       a =100; //초기화
       console.log(a); //number
       console.log(typeof(a));

       var i,j;
       i =200;
       j =300;
       var result =i + j;
       console.log(result);   

       var intnum = 100;  //정수
       var dnum = 12.345; //실수 
       var flag = true;   //boolean
       var str = null;    //값이 없다
       var str2 = "ABC";  //문자열

       console.log(typeof(intnum)); //number
       console.log(typeof(dnum));   //number
       console.log(typeof(flag));   //boolean
       console.log(typeof(str));    //object  얘도 자바처럼 최상위 객체가 있다. 
       console.log(typeof(str2));   //string

       //연산자
       //산술 (+,-,*,/,%)
       var num1 =10;
       var num2 = 3;
       document.write(num1/num2 + "<br>"); //실수범위
       document.write(num1%num2 + "<br>"); //나머지
       
       //관계 (== (===) , != , >= , <)...
       //true , false 
       
       var a =3;
       var b =5;
       document.write((a == b)+ "<br>");
       document.write((a != b)+ "<br>");
       document.write((a > b) + "<br>");

       //논리 ( && (모든 조건이 참이어야만 참), ||(둘중에 하나만 참이어도 참) )
       document.write((10>5) && (1 != 3) + "<br>");
       document.write((10>5) || (1 != 3) + "<br>");
       
       //삼항연산자
       var result2 = (4%2 == 0) ?     "짝수" : "홀수";
       document.write("<br>" + result2 + "<br>");
       
       //대입연산자( +=, -=, *= ,/= )
       var p =10;
       var k = 5;
       p+=k; // p= p+k;
       document.write("결과: " +p );
       
       var x = 5 + 5;
       var y = "5" + 5; //55 
       var z = "Hello" + 5; //Hello5
       var v = "5";
       var v2 = 5;
       document.write(v == v2);
       
    
    </script>

</head>
<body>

</body>
</html>


























